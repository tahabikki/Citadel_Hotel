# Data source selector
# json = temporary file storage in /data
# real = real database adapter
DB=json

# Media storage selector
# local = writes to backend/uploads
# cloud = cloud adapter (S3/Cloudinary)
STORAGE=local

# Optional frontend API base override
# Keep empty to use Next.js local /api routes.
NEXT_PUBLIC_API_URL=
